<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:paddingStart="12dp"
    android:paddingEnd="10dp"
    android:paddingTop="10dp"
    android:paddingBottom="10dp"
    android:background="?attr/selectableItemBackground">

    <!-- Natal planet glyph — LEFT, dark yellow -->
    <TextView
        android:id="@+id/tvNatalPlanet"
        android:layout_width="32dp"
        android:layout_height="wrap_content"
        android:textColor="#C09500"
        android:textSize="22sp"
        android:gravity="center" />

    <!-- Aspect symbol -->
    <TextView
        android:id="@+id/tvAspectSymbol"
        android:layout_width="28dp"
        android:layout_height="wrap_content"
        android:textSize="20sp"
        android:gravity="center" />

    <!-- Transit planet glyph — RIGHT, dark blue -->
    <TextView
        android:id="@+id/tvTransitPlanet"
        android:layout_width="32dp"
        android:layout_height="wrap_content"
        android:textColor="#1144CC"
        android:textSize="22sp"
        android:gravity="center" />

    <!-- Aspect name + orb -->
    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:orientation="vertical"
        android:layout_marginStart="8dp">

        <TextView
            android:id="@+id/tvAspectName"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="@color/text_primary"
            android:textSize="14sp" />

        <TextView
            android:id="@+id/tvOrb"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="@color/text_secondary"
            android:textSize="11sp" />

    </LinearLayout>

    <!-- Applying / Separating -->
    <TextView
        android:id="@+id/tvApplying"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="11sp"
        android:layout_marginEnd="4dp" />

    <!-- Clickable chevron -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="›"
        android:textSize="20sp"
        android:textColor="@color/gold"
        android:alpha="0.7" />

</LinearLayout>
